{
    "name": "trot-web",
    "version": "0.1.0",
    "private": true,
    "description": "Landing page + LLM proxy for Trot. Deployed as a single Vercel project.",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "check-drift": "../scripts/check-breed-drift.sh",
        "check": "npm run check-drift && npm run typecheck"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "@types/node": "^22.10.0"
    }
}
